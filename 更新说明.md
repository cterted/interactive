# 🔧 图结构显示问题修复说明


原代码中存在**类别映射不匹配**的问题：

- 节点数据使用的类别标识：`'goal'`, `'task'`, `'milestone'`, `'resource'`（英文）
- ECharts categories 定义：`'目标'`, `'任务'`, `'里程碑'`, `'资源'`（中文）

由于类别无法正确匹配，导致节点无法被正确分类和显示。

## 修复内容

### 1. 添加类别索引映射
```javascript
const categoryIndexMap = {
    'goal': 0,
    'task': 1,
    'milestone': 2,
    'resource': 3
};
```

### 2. 更新节点数据映射
在 `updateChart()` 函数中，将英文类别转换为索引：
```javascript
data: graphData.nodes.map(node => ({
    ...node,
    category: categoryIndexMap[node.category], // 转换为索引
    // ... 其他配置
}))
```

### 3. 修复节点选择逻辑
确保点击节点时获取原始数据（包含正确的category字段）：
```javascript
chartInstance.on('click', function(params) {
    const originalNode = graphData.nodes.find(n => n.id === params.data.id);
    if (originalNode) {
        selectedNode = originalNode;
    }
});
```

### 4. 完善树结构转换
在 `convertToTree()` 函数中保留节点的 `id` 字段，确保节点选择功能正常。

## 修复后的效果

✅ **图结构正常显示**
- 节点按类别正确着色
- 图例与节点颜色匹配
- 力导向布局正常工作

✅ **交互功能正常**
- 节点可以被正确选中
- 编辑和删除功能正常
- 拖拽功能正常

✅ **树结构正常**
- 图树切换功能正常
- 节点在两种模式下都能正确显示

## 使用方法

直接打开修复后的文件即可：
```bash
# 双击文件或使用浏览器打开
goal-decomposition-tool.html
```

如果还有任何显示问题，请检查：
1. 浏览器控制台是否有错误信息（F12打开开发者工具）
2. 确保使用现代浏览器（Chrome、Edge、Firefox最新版）
3. 确保网络连接正常（需要加载ECharts CDN）

## 技术细节

ECharts 的 graph 类型要求：
- `data` 中的节点 `category` 字段应该是**数字索引**
- 该索引对应 `categories` 数组中的位置
- `categories` 数组定义了每个类别的样式和名称

修复前后对比：

**修复前（错误）：**
```javascript
{ id: '1', name: '主目标', category: 'goal' }  // 字符串
categories: [{ name: '目标' }]  // 无法匹配
```

**修复后（正确）：**
```javascript
{ id: '1', name: '主目标', category: 0 }  // 索引
categories: [{ name: '目标' }]  // 匹配第0个类别
```
